import type { NextPage } from "next";
import Container from "../components/Container";

const About = () => {
  return (
    <Container>
      <div className={`mt-10 flex flex-col`}>
        <h2 className={`text-2xl`}>프로필 수정중</h2>
      </div>
    </Container>
  );
};

export default About;
